#include <iostream>

using namespace std ;

int main()
{
	int a[10];
	int n , b = 0;
	cin >> n;
	for(int i = 0; i < n; ++i )
	{
		cin >> a[i];
		if( i % 2 == 0)
		{
			b += a[i];
		}
	}
		cout << b << endl;
		return 0;
}

