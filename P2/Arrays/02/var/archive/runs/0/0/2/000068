#include <iostream>
using namespace std; 

int main () {

	int a[10]; int b[10]; int n;

	cin >> n;
	int s = 0;
	for (int i=0;i<n;++i) {
		cin >> a[i];
		s += a[i];
	}
	for (int i=0;i<n;++i) {
		cin >> b[i];
		s += b[i];
	}

	cout << s;

	return 0;

}
