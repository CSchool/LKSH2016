#include <iostream>
using namespace std;
int main () 
{
	int st[100];
	int N;
	int x;
	cin>> N;
	cin>> x;
	for(int i = 0;i<N;i++)
		cin>>st[i];

	for(int i = 0;i<N;i++)
		cout<<(st[i] + x)<<' ';
	return 0;
}