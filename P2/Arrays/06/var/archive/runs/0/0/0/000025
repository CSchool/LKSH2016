#include <iostream>

using namespace std;

int main() {

	int n;
	int a[100];

	cin >> n;
	for (int i = 0; i < n; ++i)
		a[i] = i + 1;

	for (int i = 0; i < n; ++i)
		cout << a[i] << ' ';
	cout << endl;

	return 0;
}