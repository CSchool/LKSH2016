#include <iostream>
using namespace std;
int main (){

	int n;
	cin >> n;
	int a[100];
	for (int i=0;i<n;++i) cin >> a[i];
	int max, x;
	max=a[0];
	x=0;
	for (int j=1;j<n;++j) {
		if (a[j]>max) {max=a[j]; x=j;};}
	cout << x;
	return 0; 
}