#include <iostream>
using namespace std;
int main (){

	int n,x;
	cin >> n >> x;
	int a[100];
	for (int i=0;i<n;++i) cin >> a[i];
	
	int y=0;

	for (int j=1;j<n;++j) {
		if (a[j]==x) y=1;}
	cout << y << ' ';
	
	return 0; 
}