#include <iostream>

using namespace std;

int main () {
	int a[100];
	int n, z, k;
	z = 0;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> a[i];
		if (a[i] > z) {
			z = a[i];
			k = i;
		}
	}
	cout << k;
}