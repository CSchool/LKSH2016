#include<iostream>

using namespace std;

int main()
{
	int m[100];
	int n;
	cin >> n;
	for (int i=0; i<n; i++)
	{
		cin >> m[i];
	}
	int max = m[0];
	int b = 0;
	for (int i=1; i < n; i++)
		if (m[i] > max){
			max = m[i];
			b = i;
	}
	cout << b << endl;
	return 0;
}