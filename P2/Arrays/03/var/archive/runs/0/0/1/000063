#include <iostream>

using namespace std;

int main () {
	int n, x, k;
	cin >> n >> x;
	int a[100];
	for (int i; i<n; i++) {
		cin >> a[i];
		if (a[i] == x) {
			k = k + 1;
		}
	}
	cout << k;
}