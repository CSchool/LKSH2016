#include <iostream>
using namespace std;

int main() {
	int n;
	int x;
	int a[100];
	int r = 0;

	cin >> n,x;

	for (int i=0;i<n;++i) {
		cin >> a[i];
		if(a[i] == x) r = 1;
	}


	cout << r;



	 system("pause");
	 return 0;
}

