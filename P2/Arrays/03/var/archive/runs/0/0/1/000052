#include <iostream>

using namespace std;

int main () {
	int a[100];
	int n, z;
	z = 0;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> a[i];
		if (a[i] > z) {
			z = a[i];}
	}
	cout << z;
}