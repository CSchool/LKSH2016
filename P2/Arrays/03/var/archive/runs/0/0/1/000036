#include<iostream>
using namespace std;
int main (){

	int n,x;
	cin >> n >> x;
	int a[1000];
	for (int i=0;i<n;++i) cin >> a[i];
	
	int y=0;

	for (int j=0;j<n;++j) {
		if (a[j]==x) ++y;}
	cout << y;
	
	return 0; 
}