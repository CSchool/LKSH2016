#include<iostream>	
using namespace std;

int main()
{
int n;
 cin >> n;
int a[100];
	for (int i=0; i<n; i++)
		cin >> a[i];
	for (int i=0; i<n; i++){
		for (int j = 2; j < a[i];j++) {
		if (a[i] % j == 0)
			a[i]=1;
		else a[i]=i;}
	}
	cout << i << endl;

	return 0;	
}