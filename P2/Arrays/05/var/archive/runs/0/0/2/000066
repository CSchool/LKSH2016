#include <iostream>

using namespace std;

int main () {
	int n, k;
	int a[100];
	cin >> n;
	for (int i = 0; i < n+2; i = i+2) {
		cin >> a[i] >> a[i+1];
		k = k + (a[i] * a[i+1]);
	}
	cout << k;
}