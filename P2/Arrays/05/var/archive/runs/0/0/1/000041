#include <iostream>

using namespace std;
int main(){
	int a[100][2];
	int n;
	int b = 0;
	cin >> n;
	for(int i = 0; i < n; ++i){
		cin >> a [i][0];
		cin >> a [i][1];
	}
	for(int i = --n ; i >= 0; --i){
		b = b + a[i][0] * a[i][1];
	}
	cout << b << endl;


	return 0;
}