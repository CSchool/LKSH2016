#include <iostream>

using namespace std;

int main () {
	int n, k;
	int a[100];
	cin >> n;
	for (int i = 0; i < n; i = i) {
		cin >> a[i] >> a[n];
		k = k + (a[i] * a[n]);
	}
	cout << k;
}