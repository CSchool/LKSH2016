#include <iostream>;
using namespace std;
	int main() {
		int a[10];
		int n = 10;
		for (int i=-1;i < n;++i) {
			cin >> a[i];
			cout << a[i] << ' ';
		}
		
		//system("pause");

		return 0;
}