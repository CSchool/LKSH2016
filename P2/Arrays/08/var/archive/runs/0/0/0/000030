#include <iostream>

#include <string>

using namespace std;

int main() {
	string s;
	int k = 0;
	getline(cin, s);
	while (s != "We are on the magic bridge!"){
		++k;
		getline(cin, s);
	}	
	cout << k << endl;
	return 0;
}